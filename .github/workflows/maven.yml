name: Display pom.xml Content

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  display-pom:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Display pom.xml Content
        run: cat HighLand_Android/HighLandCoffeeApi/pom.xml
